Obsolete documentation
======================

This folder holds phase-specific documents that are kept for historical reference but are no longer maintained. The active docs remain in the repository root and `docs`/`scripts` as applicable. Move any future deprecated markdown files here instead of deleting them outright.
